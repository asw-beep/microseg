# Core runtime dependencies.
#
# For a CPU-only install (the default for this project), install torch from the
# CPU wheel index first, otherwise pip pulls ~2.5 GB of CUDA libraries:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt

numpy>=1.24
scipy>=1.10
opencv-python>=4.8
scikit-image>=0.22
scikit-learn>=1.3
torch>=2.0
pandas>=2.0
matplotlib>=3.7
tifffile>=2023.7
PyYAML>=6.0
tqdm>=4.65

# Web UI
gradio>=4.0

# Tests
pytest>=7.4
pytest-cov>=4.1
